export default function Biglietto() {
    let nome = 'Filippo';
    let destinazione = "Dublino";
    return (
        <section>
            <p>{nome}</p>
            <p>{destinazione}</p>
        </section>
    );
}

// export default Biglietto